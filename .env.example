# Copy to .env and fill in. Do not commit .env.

# Google Sheets
GOOGLE_SHEET_ID=
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# VK user token (Standalone app, scope wall,photos + "Доступ в любое время")
# https://vkhost.github.io/
VK_API_TOKEN=

# Community API keys (local .env). On Netlify: use sheet tab "group_tokens" cell A1 instead (4KB Lambda env limit).
# npm run vk:group-tokens-template
# npm run vk:push-group-tokens   # copy .env JSON → Google Sheet
# VK_GROUP_TOKENS_SHEET=group_tokens
VK_GROUP_TOKENS={"30706989":"...","219730088":"..."}
