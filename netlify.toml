[[headers]]
  for = "/*"
    [headers.values]
    Access-Control-Allow-Origin = "*"

[build]
  environment = { NODE_VERSION = "18" }

[functions]
  included_files = ["fonts/**"]

[functions."*"]
  timeout = 60
